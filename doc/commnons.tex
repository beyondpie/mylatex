% Record some basic commands or tricks in the document.
\documentclass[a4paper]{article}

% use the two to emphasize part of the doc.
\def\mywarn#1{\textcolor{red}{#1}}
\newcommand{\myemph}[1]{\textcolor{blue}{#1}}

% use this to mark revise part in the paper
\newcommand{\myrevise}[1]{\textcolor{red}{#1}}

% use this to define vector, which can be modified to other format.
\newcommand{\myvec}[1]{\vec{#1}}

\begin{document}
Hello World!
\end{document}